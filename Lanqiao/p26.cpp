#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
    int n;
    vector<vector<int>> grid;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x, y, v, wn;
        char w;
        cin >> x >> y >> v;
        cin >> w;
        wn = (w='U')?2:(w='R')?1:

    }
    
    
    return 0;
}

